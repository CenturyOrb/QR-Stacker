import styles from './main.module.css'

const Main = () => {
	return(
		<main className={styles.main}>
			<header className={styles.header}>
				hello world
			</header>
		</main>
	);
}

export default Main
